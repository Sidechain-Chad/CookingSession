<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Cooking Classes Discovery</title>
  <style>
    /* Sets up the font and full-page background */
    body {
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Oxygen-Sans, Ubuntu, Cantarell, "Helvetica Neue", sans-serif;
      margin: 0;
      height: 100vh;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      background-color: rgb(178, 188, 230);
      background-image: url(<%= asset_path 'image.png' %>);
      background-size: cover;
      background-position: center;
    }
    /* The main card container */
    .card {
      background-color: rgba(231, 196, 160, 0.8);
      padding: 40px 50px;
      border-radius: 20px;
      box-shadow: 0 15px 35px rgba(0, 0, 0, 0.08);
      text-align: center;
      max-width: 1000px;
      width: 90%;
    }
    /* Main heading style */
    h1 {
      color:rgb(230, 114, 42);
      font-size: 2.5em;
      margin: 0 0 15px 0;
    }
    /* New heading style for Cooking basics */
    h2 {
      color:rgb(137, 211, 0);
      font-size: 10em;
      font-weight: bold;
      margin-bottom: 100px;
    }
    /* Subtitle paragraph style */
    p {
      color: #555; /*Grey*/
      font-size: 1.1em;
      margin-bottom: 30px;
    }
    /* Container for the action buttons */
    .action-buttons a {
      display: inline-block;
      margin: 8px;
      padding: 12px 30px;
      text-decoration: none;
      font-weight: 600;
      color: white;
      /* Gradient for the buttons, matching the image */
      background: linear-gradient(45deg,rgb(217, 227, 235), #8e44ad);
      border-radius: 50px;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }
    /* Hover effect for the buttons */
    .action-buttons a:hover {
      transform: translateY(-3px);
      box-shadow: 0 8px 15px rgba(0, 0, 0, 0.15);
    }
  </style>
</head>
<body>
  <h2>COOKING BASICS</h2>
  <div class="card">
    <h1>Cooking Classes Discovery</h1>
    <p>Explore, learn, and savor divine culinary delights!</p>
    <div class="action-buttons">
      <a href="/classes">Browse Classes</a>
      <a href="/contact">Contact Us</a>
    </div>
  </div>
</body>
</html>
